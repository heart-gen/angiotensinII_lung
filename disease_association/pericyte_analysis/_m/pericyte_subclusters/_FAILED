This directory's job EXITED NON-ZERO. Do not cite these files.

job: 36847215   when: 2026-01-05
script: ../_h/04.pericytes_disease_analysis.R
error: object 'prei_freq' not found  (see ../logs/analyze_pericytes.log)

WHY THE DIRECTORY LOOKS COMPLETE. All four TSVs and the figure here were written
BEFORE the throw -- the failing statement is the function's final `return`, after
every `fwrite`. So nothing on disk distinguished this from a finished run, which
is exactly how these tables came to be read as results. That is defect P1-17.

WHAT IS ALSO WRONG WITH THEM, independent of the crash:
  * `session_info()` never ran. The `#### Reproducibility information ####` block
    sits after the failing call, so this module has NO R package versions on file
    -- the only step in it without them.
  * The cell-type labels are SUPERSEDED (pre-relabel).
  * There are 670 pericytes and only 24 Control cells.
  * The upstream `X` is un-logged.
  See writings/TODO.md P2-21, P2-22, P2-23, P2-24.

The one-character typo was fixed 2026-09-07 and step_4.sh now writes this marker
automatically on any non-zero exit (and removes it on success). These files were
NOT regenerated: whether this module is rebuilt or retired is the open decision in
P2-24, and re-running it would only produce superseded labels with a session
record attached.

-------------------------------------------------------------------------------
UPDATE 2026-09-07 -- P2-22 and P2-23 investigated. The rebuild-or-retire question
is now answered by measurement rather than left open: RETIRE. Evidence in
_m/transfer_diagnostics/ (written by _h/05.transfer_diagnostics.py).

1. THE MATRICES ARE NOT WHAT THE PIPELINE INTENDED (layer_audit.tsv).
   `01.subset_data.py` called `normalize_total(adata, layer="counts")`, which
   normalises the COUNTS LAYER in place and leaves X alone -- the opposite of the
   intent on both matrices:
     * layers["counts"] became CP10K-normalised and non-integer, and that is the
       layer scANVI was set up on; and
     * X was log1p'd while still RAW, so every HVG call, PCA and neighbour graph
       in this module ran on log1p(raw counts) with no depth normalisation.
   `03.transfer_labels.py` then overwrote X with those normalised counts while
   leaving uns['log1p'] in place. Shipped clustered_data.h5ad X runs to 1290.76;
   a log1p CP10K matrix cannot exceed log1p(1e4) = 9.21.

2. THE TRANSFER DOES NOT REPRODUCE THE QUERY'S OWN STRUCTURE
   (leiden_vs_transferred_label.tsv, concordance.json). Against the de novo
   Leiden partition: adjusted Rand 0.369, normalized MI 0.466. One Leiden cluster
   splits 32/57% across two labels; one label spans two Leiden clusters. Median
   transfer confidence 0.628, and 31% of cells fall below 0.5. Two of the six
   labels are 8 and 3 cells at median confidence 0.385 and 0.222.

3. IT CANNOT BE REPAIRED FROM WHAT IS ON DISK. The query side is fixable -- the
   upstream ipf_dataset.h5ad X is raw integers (1-58), so those counts exist and
   were destroyed, not missing; that is fixed in code. The REFERENCE is not:
   ref_hvg layers["counts"] is bit-identical in range to layers["soupX"]
   (0.0021-908.71, non-integer), i.e. the reference's "counts" is the ambient-
   corrected matrix and no raw count matrix for it exists in this repository.
   scANVI's NB likelihood cannot be trained correctly without one.

4. AND THE COHORT WOULD STILL NOT SUPPORT THE CLAIM: 670 cells, of which
   IPF 578 / COPD 68 / Control 24.

The code defects are fixed so a future rebuild starts correct (P2-22, P2-23), but
a rebuild is BLOCKED on regenerating the reference from original counts. Nothing
here should be cited in the meantime.
